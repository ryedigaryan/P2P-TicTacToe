package tictactoe.app.flow.item.listener;

import genericapp.AbstractAppFlowItemStateChangeListener;

// no need to remove MainMenu instance from TicTacToeAppFlow because we will need it frequently
public class MainMenuStateChangeListener extends AbstractAppFlowItemStateChangeListener {
}
